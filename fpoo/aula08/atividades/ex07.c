#include <stdio.h>
#include <locale.h>
#include <stdlib.h>
#include <stdbool.h>

int fato(int n, int fato, int i);

	
int main (){
	int num;
	printf("Digite um número:\n");
	scanf("%d", &num);
	printf("o fatorial desse número é %d", fato);
}

int fato(int n, int fato, int i){
	for(i = 0; i < num; i++){
		n * (n-1) = fato;
		
		 
	}
}
